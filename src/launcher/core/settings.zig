// TODO + Idea :

// Write a settings.cfg
// Windows projects : Stored in Documents/Projects by default
// Linux projects : Stored in Documents/Projects by default
// Possibility to change the projects folder and save it
// Settings will also have other things like the shader rebuild for the engine/projects if anything is corrupted
// Maybe a project repair too (need to see when launcher functions are done)
